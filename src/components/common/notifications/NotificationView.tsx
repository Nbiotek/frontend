'use client';

import NotificationsCard from './components/NotificationsCard';

const NotificationView = () => {
  return <NotificationsCard viewType="mobile" />;
};

export default NotificationView;
